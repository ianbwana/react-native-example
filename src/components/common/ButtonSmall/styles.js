import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    buttonHeaderHeight: {
        height: 40
    }, buttonHeaderWidth: {
        width: 80
    },
    buttonHeight: {
        height: 30
    },
    buttonWidth: {
        width: '50%'
    }
});

export default styles;
