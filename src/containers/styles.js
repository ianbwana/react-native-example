import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    alignedCenter: { alignItems: 'center' },
    alignedStart: { alignItems: 'flex-start' },
    justifiedCenter: { justifyContent: 'center' },
    justifiedStart: { justifyContent: 'flex-start' },
    viewColor: { color: '#07271F' }
});

export default styles;
